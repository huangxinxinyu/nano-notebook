# Scope multi-Provider behavior by model capability

The first release will exercise multiple Providers for Generation and reuse multimodal Generation Providers for Vision. Embedding supports multiple adapters but binds each retrieval-index version to one embedding model and switches only through reindexing; Transcription initially has one accepted Provider because fallback output can change timestamp-based citations. Gateway-level support alone does not make a Provider/capability combination product-supported.
